# Node / TS
node_modules/
.pnpm-store/
dist/
.next/
out/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
.venv/
.uv/
uv.lock.*
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# Env / secrets
.env
.env.*
!.env.example

# Playwright
test-results/
playwright-report/
playwright/.cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.log
.vercel

# An unrelated v0 scratch project ("my-v0-project") that lives in this working tree but is not
# part of the monorepo — not a pnpm workspace member, not built, not deployed. Ignored so a
# `git add -A` can't sweep it into a commit again (it did, in PR #49).
compute/
